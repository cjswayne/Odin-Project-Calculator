function operate(operator, a, b){
    if (operator == "+"){
        return (a + b)
    } else if (operator == "-"){
        return (a-b)
    }else if (operator == "*"){
        return (a*b)
    }else if (operator == "/"){
        return (a/b)
    }
}

